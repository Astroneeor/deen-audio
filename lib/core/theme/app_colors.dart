import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF0A0A0F);
  static const Color surface = Color(0xFF13131A);
  static const Color surfaceVariant = Color(0xFF1C1C26);
  static const Color sidebar = Color(0xFF0F0F16);

  // Accent
  static const Color gold = Color(0xFFC9A84C);
  static const Color goldLight = Color(0xFFE0C068);
  static const Color goldDark = Color(0xFF9E7A2E);

  // Text
  static const Color textPrimary = Color(0xFFF0EEE8);
  static const Color textSecondary = Color(0xFF9E9C96);
  static const Color textMuted = Color(0xFF5A5856);

  // States
  static const Color error = Color(0xFFCF6679);
  static const Color success = Color(0xFF4CAF82);

  // Dividers / borders
  static const Color divider = Color(0xFF22222E);
  static const Color border = Color(0xFF2A2A38);

  // Player bar
  static const Color playerBar = Color(0xFF0F0F18);

  // Active/selected
  static const Color activeIndicator = Color(0xFFC9A84C);
}
